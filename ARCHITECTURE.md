# מפת הקוד — Ori Fitness App

המסמך הזה נועד למטרה אחת: **כשמשהו נשבר, לדעת תוך שניות לאן להסתכל.**

---

## 1. סדר ההפעלה — מה רץ, ובאיזה סדר

כשנפתחת האפליקציה, `js/app.js` מריץ את `main()` בסדר הזה. **הסדר קריטי** —
כל שלב מניח שהקודם הסתיים:

| # | שלב | קובץ | אם זה נכשל |
|---|---|---|---|
| 1 | רישום Service Worker | `app.js` → `sw.js` | האפליקציה לא תעבוד אופליין |
| 2 | **שער הכניסה** | `gate.js` → `auth.js` | **תקוע במסך התחברות** |
| 3 | פתיחת מסד הנתונים | `db.js` | הודעת שגיאה אדומה, כלום לא נטען |
| 4 | אתחול מודולים | כל השאר | מסך מסוים ריק או לא מגיב |
| 5 | חיבור הניווט | `app.js` → `initNav` | **לחיצה על טאב לא עושה כלום** |
| 6 | אשף התחלה (אם צריך) | `onboarding.js` | משתמש חדש לא מוגדר |

> ⚠️ **מלכודת שכבר תפסה אותנו:** אם שלב 2 מסתיים מוקדם מדי, השער נסגר אבל
> שלבים 3–6 לא רצים. התוצאה: **רואים את האפליקציה אבל שום דבר לא מגיב.**
> לכן `initGate()` מחזיר Promise שנפתר **רק ברגע האישור**.

---

## 2. מפת המודולים — מי אחראי על מה

### תשתית (לא נוגעים בלי סיבה טובה)

| קובץ | אחראי על |
|---|---|
| `db.js` | IndexedDB — קריאה, כתיבה, גרסאות. **כל** הנתונים עוברים דרכו |
| `ui.js` | לבני בניין משותפות: `el()`, גיליונות, טוסטים, תאריכים, קונפטי |
| `app.js` | הפעלה, ניווט בין מסכים, מעבר יום בחצות |
| `charts.js` | ציור גרפים ב-SVG. אין תלות בנתונים — מקבל מספרים ומצייר |

### כניסה והרשאות

| קובץ | אחראי על |
|---|---|
| `firebase-config.js` | פרטי החיבור + רשימת המנהלים (`ADMIN_EMAILS`) |
| `gate.js` | מחליט: להציג דף נחיתה או לשחרר לאפליקציה |
| `auth.js` | התחברות Google, ובדיקת `pending`/`approved`/`blocked` |

### מסכים

| מסך | קובץ ראשי | קבצי עזר |
|---|---|---|
| בית | `dashboard.js` | `challenge.js`, `badges.js` |
| אימון | `workouts.js` | `routines.js`, `cardio.js`, `records.js` |
| תזונה | `nutrition.js` | `mealplan.js` |
| התקדמות | `progress.js` | `bodyweight.js`, `charts.js` |
| הגדרות | `settings.js` | `backup.js`, `onboarding.js` |

---

## 3. אבחון מהיר — לפי הסימפטום

| מה שרואים | איפה לחפש |
|---|---|
| תקוע במסך התחברות | `auth.js` — הודעת השגיאה על המסך מציגה קוד מדויק |
| נכנס, אבל שום לחיצה לא עובדת | `app.js` → `main()`, סעיף 1 למעלה |
| לחיצה על טאב לא מחליפה מסך | `app.js` → `showScreen` / `initNav` |
| כפתור "חזור" סוגר את האפליקציה | `app.js` → `showScreen`, ה-`pushState` |
| נתונים נעלמו | `db.js` — ואז `backup.js` לשחזור |
| לא עובד בלי אינטרנט | `sw.js` — בדוק שהקובץ ברשימת `APP_SHELL` |
| מספרים שגויים באימון | `workouts.js` → `calcVolume` / `countSets` |
| שיא אישי לא מזוהה | `records.js` → `checkPR` |
| קלוריות לא מתעדכנות | `nutrition.js` → `renderNutrition` |
| גרף ריק | `charts.js` — צריך **2+ נקודות**, זו התנהגות מכוונת |
| בטלפון נראה שונה מהמחשב | `index.html` — תגית viewport + סקריפט ההקטנה |

---

## 4. איפה נשמר כל דבר

| מה | איפה | עובר בין מכשירים? |
|---|---|---|
| אימונים, תזונה, משקל, תמונות | **IndexedDB במכשיר** | ❌ **לא** |
| הגדרות (שם, יעדים, אתגר) | IndexedDB (`settings`) | ❌ לא |
| מי מאושר להיכנס | **Firestore (ענן)** | ✅ כן |
| ההחלטה האחרונה על הרשאה | `localStorage` | ❌ לא (גיבוי לאופליין) |

> ⚠️ **הנתונים אינם בענן.** Firebase מטפל רק בשאלה מי נכנס.
> מחיקת נתוני הדפדפן = אובדן הכל. **הגיבוי הוא רשת הביטחון היחידה.**

---

## 5. כללים לשמירה על הסדר

1. **קובץ אחד = אחריות אחת.** פיצ'ר חדש → מודול חדש, לא הרחבה של קיים.
2. **כל מודול פותח בכותרת** שמסבירה מה הוא עושה ולמה.
3. **מקטעים פנימיים** מסומנים ב-`/* ---------- שם ---------- */`.
4. **קובץ JS חדש חייב להתווסף ל-`APP_SHELL` ב-`sw.js`** — אחרת האפליקציה
   תישבר אופליין. *(זה כבר קרה: `challenge.js` ו-`badges.js` נשכחו.)*
5. **העלה את `CACHE_VERSION`** בכל שינוי משמעותי.
6. **הערות מסבירות "למה", לא "מה".** הקוד כבר אומר מה הוא עושה.

---

## 6. פקודות שימושיות

בדיקה מקומית (פורט 8080):
```bash
powershell -NoProfile -ExecutionPolicy Bypass -File serve.ps1 -Port 8080
```

פרסום — האתר מתעדכן לבד תוך כדקה:
```bash
git add . && git commit -m "תיאור" && git push
```
