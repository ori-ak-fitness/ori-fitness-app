# Ori Fitness App

אפליקציית כושר אישית בעברית (RTL), PWA. אורי אינו מפתח — הוא הבעלים והמשתמש.

**קרא קודם `ARCHITECTURE.md`** — מפת הקוד: סדר ההפעלה, מי אחראי על מה,
וטבלת סימפטום→קובץ.

## עבודת עיצוב — חובה

**כל בקשה שנוגעת לאיך האפליקציה נראית או מרגישה — קרא והרץ את
`.claude/skills/app-design/SKILL.md` לפני שאתה נוגע בקוד.**

זה חל גם על בקשות שנשמעות זעירות ("תגדיל את הריבועים", "זה נראה עמוס",
"תזיז את זה למעלה") — דווקא הן אלה שיוצאות שגויות בלעדיו. הקובץ מכיל את
מערכת העיצוב האמיתית, את רשימת הביקורת העצמית, ואת נוהל הבדיקה בדפדפן.

אל תסתמך על הזיכרון שלך לגבי איך מסך נראה. תקרא את `css/styles.css`, את
המבנה ב-`index.html`, ואת פונקציית הרינדור ב-`js/` — במסכים רבים מה שרואים
בפועל נבנה ב-JS ולא קיים ב-HTML הסטטי.

## מגבלות שלא משתנות

- **בלי שלב בנייה, בלי npm, בלי פריימוורק.** אין node על המכונה. ES modules
  בלבד, כתוב ביד.
- **בלי media queries לפי רוחב.** רוחב אחד זורם. לבדוק ב-390 וב-430.
- **קובץ `js/` חדש חייב להיכנס ל-`APP_SHELL` ב-`sw.js`**, ולהעלות
  `CACHE_VERSION` — אחרת האפליקציה נשברת אופליין.
- **מפתח הגדרה חדש חייב להיכנס ל-`RESET_SETTING_KEYS` ב-`settings.js`.**

## כל שינוי נגמר ככה

לבדוק בדפדפן (`preview_start`, שם `ori-fitness`, פורט 8080) — לא לקרוא את
הקוד בחזרה ולהניח. ה-Service Worker הוא cache-first, ולכן חייבים לנקות
caches ולבטל רישום SW אחרי כל עריכה, אחרת השינוי פשוט לא נטען ונראה שהוא
נכשל. אחר כך commit ו-push באותו תור — ההרשאה שמורה, האתר מתעדכן לבד,
והעדכון מתקין את עצמו אצל אורי.

## איך לדבר איתו

בעברית, קצר, ולהתחיל מהתוצאה. מספרים קונקרטיים ("56 ← 59") במקום "שיפרתי".
להגיד במפורש מה **לא** נעשה. בבקשת עיצוב מעורפלת — להחליט ולבנות, לא לשאול
פעמיים; הוא מגיב מהר ומשנה את דעתו בחופשיות, וזו שיטת העבודה. אם תיקון
פספס, לשנות מאפיין **אחר** ולא לדחוף שוב על אותו אחד.
